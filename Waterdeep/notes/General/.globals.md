```ui
// Main stats
character_name("Magister Septimus el Blau")
background("Charlatan")
class_("Sorcerer")
species("Gnome")
subclass("Wild Magic")
level(1)

// Ability scores
strength(8)
dexterity(16)
constitution(12)
intelligence(16)
wisdom(10)
charisma(20)

// Ability modifiers
// Ability modifiers
strength_mod(((strength - 10) - ((strength - 10) % 2)) / 2)
dexterity_mod(((dexterity - 10) - ((dexterity - 10) % 2)) / 2)
constitution_mod(((constitution - 10) - ((constitution - 10) % 2)) / 2)
intelligence_mod(((intelligence - 10) - ((intelligence - 10) % 2)) / 2)
wisdom_mod(((wisdom - 10) - ((wisdom - 10) % 2)) / 2)
charisma_mod(((charisma - 10) - ((charisma - 10) % 2)) / 2)

// Proficiency
proficiency_bonus(2)

if(level >= 5) {
  proficiency_bonus = 3
}
if(level >= 9) {
  proficiency_bonus = 4
}
if(level >= 13) {
  proficiency_bonus = 5
}
if(level >= 17) {
  proficiency_bonus = 6
}

// Combat
base_armor_class(10)
shield_equipped(false)
shield_bonus(0)
armor_class(base_armor_class + dexterity_mod + shield_bonus)

hp_current(100)
hp_temp(0)
hp_max(100)

hit_dice_spent(0)
hit_dice_max(level)

initiative(dexterity_mod)
speed(25)
size("Small")
passive_perception(10 + wisdom_mod)

// Saving throw proficiencies
save_prof_strength(false)
save_prof_dexterity(false)
save_prof_constitution(true)
save_prof_intelligence(false)
save_prof_wisdom(false)
save_prof_charisma(true)

save_strength(strength_mod)
save_dexterity(dexterity_mod)
save_constitution(constitution_mod)
save_intelligence(intelligence_mod)
save_wisdom(wisdom_mod)
save_charisma(charisma_mod)

if(save_prof_strength) {
  save_strength = strength_mod + proficiency_bonus
}
if(save_prof_dexterity) {
  save_dexterity = dexterity_mod + proficiency_bonus
}
if(save_prof_constitution) {
  save_constitution = constitution_mod + proficiency_bonus
}
if(save_prof_intelligence) {
  save_intelligence = intelligence_mod + proficiency_bonus
}
if(save_prof_wisdom) {
  save_wisdom = wisdom_mod + proficiency_bonus
}
if(save_prof_charisma) {
  save_charisma = charisma_mod + proficiency_bonus
}

// Skill proficiencies
acrobatics_prof(false)
animal_handling_prof(false)
arcana_prof(true)
athletics_prof(false)
deception_prof(true)
history_prof(false)
insight_prof(false)
intimidation_prof(false)
investigation_prof(false)
medicine_prof(false)
nature_prof(false)
perception_prof(false)
performance_prof(false)
persuasion_prof(true)
religion_prof(false)
sleight_of_hand_prof(true)
stealth_prof(false)
survival_prof(false)

// Skills
acrobatics(dexterity_mod)
animal_handling(wisdom_mod)
arcana(intelligence_mod)
athletics(strength_mod)
deception(charisma_mod)
history(intelligence_mod)
insight(wisdom_mod)
intimidation(charisma_mod)
investigation(intelligence_mod)
medicine(wisdom_mod)
nature(intelligence_mod)
perception(wisdom_mod)
performance(charisma_mod)
persuasion(charisma_mod)
religion(intelligence_mod)
sleight_of_hand(dexterity_mod)
stealth(dexterity_mod)
survival(wisdom_mod)

if(acrobatics_prof) {
  acrobatics = dexterity_mod + proficiency_bonus
}
if(animal_handling_prof) {
  animal_handling = wisdom_mod + proficiency_bonus
}
if(arcana_prof) {
  arcana = intelligence_mod + proficiency_bonus
}
if(athletics_prof) {
  athletics = strength_mod + proficiency_bonus
}
if(deception_prof) {
  deception = charisma_mod + proficiency_bonus
}
if(history_prof) {
  history = intelligence_mod + proficiency_bonus
}
if(insight_prof) {
  insight = wisdom_mod + proficiency_bonus
}
if(intimidation_prof) {
  intimidation = charisma_mod + proficiency_bonus
}
if(investigation_prof) {
  investigation = intelligence_mod + proficiency_bonus
}
if(medicine_prof) {
  medicine = wisdom_mod + proficiency_bonus
}
if(nature_prof) {
  nature = intelligence_mod + proficiency_bonus
}
if(perception_prof) {
  perception = wisdom_mod + proficiency_bonus
}
if(performance_prof) {
  performance = charisma_mod + proficiency_bonus
}
if(persuasion_prof) {
  persuasion = charisma_mod + proficiency_bonus
}
if(religion_prof) {
  religion = intelligence_mod + proficiency_bonus
}
if(sleight_of_hand_prof) {
  sleight_of_hand = dexterity_mod + proficiency_bonus
}
if(stealth_prof) {
  stealth = dexterity_mod + proficiency_bonus
}
if(survival_prof) {
  survival = wisdom_mod + proficiency_bonus
}

// Species rules
if(shield_equipped) {
  shield_bonus = 2
}
if(!shield_equipped) {
  shield_bonus = 0
}

if(species == "Human") {
  speed = 30
  size = "Medium"
}
if(species == "Elf") {
  speed = 30
  size = "Medium"
}
if(species == "Dwarf") {
  speed = 25
  size = "Medium"
}
if(species == "Halfling") {
  speed = 25
  size = "Small"
}
if(species == "Gnome") {
  speed = 25
  size = "Small"
}

// Spellcasting
spellcasting_ability("Charisma")
spell_attack_bonus(charisma_mod + proficiency_bonus)
spell_save_dc(8 + charisma_mod + proficiency_bonus)

// Spell slots
spell_slot_total([2,0,0,0,0])
spell_slot_expended([0, 0, 0, 0, 0])

spell_slot_remaining([
  spell_slot_total[0] - spell_slot_expended[0],
  spell_slot_total[1] - spell_slot_expended[1],
  spell_slot_total[2] - spell_slot_expended[2],
  spell_slot_total[3] - spell_slot_expended[3],
  spell_slot_total[4] - spell_slot_expended[4]
])

sorcery_points(2)
sorcery_points_max(2)
```